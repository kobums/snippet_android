<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name=".SnippetApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Snippet">

        <!-- ISBN 바코드 스캐너(Google Code Scanner) 모듈 설치 시점 다운로드 -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="barcode_ui" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Snippet">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 독서 세션 타이머 알림의 액션 버튼(일시정지/재개/완료) 수신기.
             타이머는 wall-clock epoch 차이로 경과를 계산하고 알림 카운트업은 시스템
             크로노미터가 굴리므로, 시간을 세기 위한 Foreground Service가 필요 없다.
             (FGS를 두지 않아 specialUse 선언·Play 시연 영상 심사·Android 15 FGS
              실행 시간 제한이 모두 해당되지 않는다.) -->
        <receiver
            android:name=".reading.ReadingTimerActionReceiver"
            android:exported="false" />

        <!-- 홈 화면 위젯 (오늘의 스니펫) -->
        <receiver
            android:name=".ui.widget.SnippetWidgetReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/snippet_widget_info" />
        </receiver>

        <!-- FCM 푸시 알림 수신 서비스 -->
        <service
            android:name=".fcm.SnippetMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- FCM 기본 알림 채널 및 아이콘 -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="snippet_push" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />

        <!-- FileProvider: 카메라 촬영 결과 임시 파일 Uri 공유 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
